version: '3.8'

services:
  ip-sakti:
    build: .
    ports:
      - "8000:8000"
      - "8501:8501"
    environment:
      - PORT=8501
      - BACKEND_URL=http://localhost:8000
    env_file:
      - .env
